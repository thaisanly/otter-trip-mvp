# Environment
NODE_ENV=development

# Application Configuration
# Copy this file to .env.local and update with your values
NEXT_PUBLIC_APP_NAME="Otter Trip"
APP_BASE_URL="http://localhost:3000"

# Database - REQUIRED
DATABASE_URL="postgresql://postgres:postgres@host.docker.internal:5432/otter_trip_dev?schema=public"

# JWT Security - REQUIRED
# SECURITY: Generate a strong 32+ character secret for production
# Example: openssl rand -base64 32
JWT_SECRET="your-32-plus-character-jwt-secret-key-change-this-immediately"

# Email configuration - REQUIRED
# Choose email provider: 'resend' or 'smtp'
EMAIL_PROVIDER=smtp
ADMIN_EMAIL=contact@ottertrip.com

# Resend configuration (when EMAIL_PROVIDER=resend)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxx

# SMTP configuration (when EMAIL_PROVIDER=smtp) - ALL REQUIRED when using SMTP
# For local development with Mailpit: host.docker.internal:1025
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@ottertrip.com